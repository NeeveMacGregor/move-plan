import React from "react";
import ReactDOM from "react-dom/client";
import MovePlan from "./MovePlan.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MovePlan />
  </React.StrictMode>
);
